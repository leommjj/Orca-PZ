{
  "name": "orca-PZ",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "cross-env NODE_ENV=production tsc && vite build && cross-env BUILD_EDITOR=true vite build",
    "build:plugin": "vite build",
    "build:editor": "cross-env BUILD_EDITOR=true vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@orca/types": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "cross-env": "^7.0.3",
    "typescript": "^5.0.0",
    "vite": "^4.0.0"
  }
}

